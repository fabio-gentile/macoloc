{{ form_start(form, {'attr': {'class': 'flex gap-4'}}) }}
{{ form_label(form, 'Rechercher', {'label_attr': {'class': 'sr-only'}}) }}
{{ form_widget(form) }}
<button class="btn btn-primary" type="submit">
    Rechercher
</button>
{{ form_end(form) }}
