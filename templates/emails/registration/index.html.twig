{% extends 'emails/base.html.twig' %}

{% block title %}Confirmation E-mail pour Macoloc{% endblock %}

{% block body %}
    <div class="flex flex-col gap-8">
        <a href="{{ url('app_homepage') }}" class="flex flex-col gap-2 justify-center items-center">
            <img src="{{ email.image('@images/logo.png') }}" class="object-fit w-28 md:w-40" alt="Logo Macoloc">
            <h1 class="text-primary font-semibold text-xl sm:text-2xl lg:text-3xl">Macoloc</h1>
        </a>
        <h1 class="font-semibold text-2xl text-foreground">Confirmation E-mail pour Macoloc</h1>
        <p class="text-foreground">
            Veuillez confirmer votre adresse e-mail en cliquant sur le lien suivant : <br><br>
            <a href="{{ signedUrl|raw }}" class="text-primary underline">Confirmer mon adresse e-mail</a>.<br>
            Ce lien expirera dans {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
        </p>
    </div>
{% endblock %}
