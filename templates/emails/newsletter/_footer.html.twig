<footer class="flex flex-col gap-8 py-6 bg-ternary">
    <div class="flex flex-col gap-4 md:flex-row flex-wrap items-center justify-center md:gap-8 wrapper">
        <a href="{{ absolute_url(url('app_homepage')) }}" class="text-center text-sm text-muted">Accueil</a>
        <a href="{{ absolute_url(url('app_search_housing')) }}" class="text-center text-sm text-muted">Rechercher</a>
        <a href="{{ absolute_url(url('app_publish_housing')) }}" class="text-center text-sm text-muted">Publier une annonce</a>
        <a href="{{ absolute_url(url('app_contact')) }}" class="text-center text-sm text-muted">Contact</a>
        <a href="{{ absolute_url(url('app_privacy')) }}" class="text-center text-sm text-muted">Politique de confidentialité</a>
        <a href="{{ absolute_url(url('app_terms')) }}" class="text-center text-sm text-muted">Conditions d’utilisation</a>
        <a href="{{ absolute_url(url('app_newsletter_unsubscribe', {
            'ut': unsubscribe_token,
            'nt': newsletter_token
        })) }}" class="text-center text-sm text-muted font-semibold">Se désinscrire</a>
    </div>
    <p class="text-center text-sm text-muted">© 2024 Macoloc</p>
</footer>
